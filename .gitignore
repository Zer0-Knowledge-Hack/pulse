# Dependencies
node_modules/
.pnpm-store/

# Build
dist/
build/
.turbo/
.output/
.vinxi/
out/
.wrangler/

# Env and secrets
.env
.env.*
!.env.example
.studio/

# Local SDD / skill-registry cache
.atl/

# Logs and OS
*.log
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/*
!.vscode/extensions.json

# Claude Code personal config. Hooks are opt-in per developer, so the file
# that activates one is never shared. Committed hook scripts, if any, live
# in .claude/hooks/ and are activated only by a local settings file.
.claude/settings.local.json
packages\.env
apps\web\.env
apps\web\.env.example