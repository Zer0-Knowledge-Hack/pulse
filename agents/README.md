# Seed agents

Owned by workstream **A**. Keep BNB Agent Studio projects here (`bag init`).

Day-0 marketplace browse uses fixtures in `packages/indexer/fixtures/featured.json`. When an agent is live on BSC, publish its `tokenId`, endpoint, and category into that file (or the later indexer) — do not edit `apps/web`.
